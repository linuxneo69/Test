Thank you for the suggestion.

We tested $(labels.model_user_id) as requested, but the behavior is still not correct in our PromQL-based alert policies. In our case, this value does not resolve to the model name in the notification. Instead, it is either rendered literally in the email subject/body or does not expand as expected.

A quick summary of the current behavior:

${resource.project} now works correctly for the project name in the notification.

${labels.model_user_id} / $(labels.model_user_id) does not resolve to the model name.

The model field still appears as null in some alerts, and in other cases the variable is printed as-is in the email subject/body.

What we have verified:

In Metrics Explorer, the PromQL query results do include model_user_id.

The issue is only in alert notification variable expansion.

We have already tried multiple variable formats, including:

${metric.label.model_user_id}

${labels.model_user_id}

$(labels.model_user_id)

${metric_or_resource.label.model_user_id}

None of these have successfully expanded the model name in the alert notification.

Current status:

Burndown alert is working for the project name using ${resource.project}.

Other PromQL alerts such as PT Spillover, HTTP error code alerts, Non-200 alerts, and predictive alerts still do not resolve the model name correctly.

Could you please help confirm one of the following:

Whether model_user_id is expected to be accessible through a different variable namespace for PromQL alert policies.

Whether there is a known limitation or bug affecting model_user_id expansion in alert notifications.

Whether there is a supported workaround to reliably show the model name in the subject and documentation sections.

Screenshots attached:

[Screenshot 1: Email showing $(labels.model_user_id) rendered literally in subject/body]

[Screenshot 2: Metrics Explorer output showing model_user_id present in the query result]

[Screenshot 3: Working alert showing ${resource.project} resolves correctly]

We would appreciate any guidance you can provide so we can finalize a stable alert format for Vertex AI monitoring.
---


Quick update: we’re still working through the Vertex AI alert label issue with Google support, but we do not have a final fix yet. The main problem remains that the model label is not expanding correctly in some PromQL-based alert notifications, even though the metric output shows the label in Metrics Explorer. I have been coordinating with my colleague xyz, who has access to the support case, and I’ve asked them to arrange another call with Google support so we can push for a quicker resolution. I will share the next update as soon as we get feedback from Google.
